# Modules

Hiện có `health` và `auth`. Auth bao gồm đăng nhập/phiên/đổi mật khẩu, dùng Prisma và guard mặc định bảo vệ API. Controller xử lý HTTP/cookie; service sở hữu quy tắc xác thực. Chưa có CRUD nghiệp vụ HRM hoặc quản trị tài khoản.
